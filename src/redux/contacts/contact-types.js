const ADD_CONTACT = 'contact/Add';
const DELETE_CONTACT = 'contact/Delete';
const FILTER_CONTACTS = 'contact/Filter';

const typesAction = {
  ADD_CONTACT,
  DELETE_CONTACT,
  FILTER_CONTACTS,
};

export default typesAction;
